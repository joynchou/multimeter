public.h文件中有一些全局宏，可以用来开启和关闭调试模式

本设计采用了类似MVP架构的设计方式，将逻辑层和view层分离，之间使用presenter层来衔接

本设计中的逻辑层即为三个表，即电压表，电流表，欧姆表，

view层在ui相关的文件中

presenter层则只有一个文件，userGui.c，该文件用于衔接view和model层

这样做的好处是可以时代码解耦，同时易于扩增功能
